#include "main.h"
#include <stdio.h>
/**
 * main - tests function that prints if interger is positive/negative
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
